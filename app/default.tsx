'use client';

// This is a default component for the default slot in the layout
// It doesn't need to render anything as we're handling content in page.tsx
export default function Default() {
  return null;
}
