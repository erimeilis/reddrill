// Entity slot for /templates route (no specific template selected)
// Renders nothing - only show entity content when a specific template is selected
export default function TemplatesEntityPage() {
  return null;
}
