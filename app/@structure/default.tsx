'use client';

export default function StructureDefault() {
  // This component doesn't need to handle redirection anymore
  // The redirection is now handled only in the main page.tsx component

  // This component doesn't render anything visible
  // It just handles redirection logic
  return null;
}
