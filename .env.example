# ============================================
# RedDrill Environment Configuration
# ============================================
# Copy this file to .env and configure for local development
# cp .env.example .env

# ============================================
# Database Configuration
# ============================================
# Local development uses SQLite via Prisma
# Production uses Cloudflare D1 (configured in wrangler.toml)

# Local SQLite database for development
DATABASE_URL="file:./dev.db"

# ============================================
# Cloudflare Configuration
# ============================================
# These are configured in wrangler.toml, not environment variables:
# - AI binding (Cloudflare Workers AI for translations)
# - D1 binding (Cloudflare D1 for audit trail)
#
# For production deployment:
# 1. Update wrangler.toml with your account_id
# 2. Create D1 database: npm run db:create
# 3. Update database_id in wrangler.toml
# 4. Run migrations: npm run db:migrate:remote

# ============================================
# Mandrill Configuration
# ============================================
# Mandrill API key is NOT configured via environment variables
# Users connect their Mandrill account at runtime via the UI
# The API key is stored in browser localStorage and IndexedDB

# ============================================
# Optional: Audit Trail
# ============================================
# Audit trail requires D1 database setup
# See README.md "Audit Trail Activation" section for details
# Run: npm run db:migrate:local (for local development)
# Run: npm run db:migrate:remote (for production)
