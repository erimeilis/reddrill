# ============================================
# RedDrill Environment Configuration
# ============================================
# Copy this file to .env and configure for local development
# cp .env.example .env

# ============================================
# Database Configuration
# ============================================
# Local development uses SQLite via Prisma
# Production uses Cloudflare D1 (configured via deploy script)

# Local SQLite database for development
DATABASE_URL="file:./dev.db"

# D1 Database ID (optional - auto-created if not set)
# Get existing ID with: wrangler d1 list
# D1_DATABASE_ID=your-d1-database-id

# ============================================
# Cloudflare Deployment
# ============================================
# Custom domain for production deployment (optional)
# Leave empty to use default workers.dev subdomain
# Example: reddrill.example.com
CUSTOM_DOMAIN=

# ============================================
# Mandrill Configuration
# ============================================
# Mandrill API key is NOT configured via environment variables
# Users connect their Mandrill account at runtime via the UI
# The API key is stored in browser localStorage and IndexedDB

# ============================================
# Audit Trail
# ============================================
# Audit trail requires D1 database setup
# The deploy script handles database creation and migrations automatically
